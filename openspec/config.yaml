schema: spec-driven

context: |
  Project: misconfig — dotfiles management via chezmoi
  Target: CachyOS (Arch rolling), btrfs, Niri Wayland, PipeWire
  Machine: Acer Aspire R3-431T / Intel Celeron 3205U / 3.7 GiB RAM
  VCS: chezmoi (not yet initialized, no git repo)
  Language: configuration files (TOML, conf, zshrc, etc.) — no code compiler

rules:
  proposal:
    - Include rollback plan for risky changes
    - Target the CachyOS/Arch environment with btrfs snapshot awareness
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Document config file locations and chezmoi template logic
    - Note systemd, Niri, or PipeWire dependencies where relevant
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing project structure and chezmoi conventions
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas

testing:
  strict_tdd: false
  reason: >
    No test runner or programming language framework detected.
    This is a dotfiles/configuration management repo using chezmoi.
    Config validation relies on target application syntax checks
    (e.g., niri validate, zsh -n, pw-cli).
  detected: "2026-06-09"

  runner:
    available: false
    command: ""
    framework: none

  layers:
    unit:
      available: false
      tool: "—"
    integration:
      available: false
      tool: "—"
    e2e:
      available: false
      tool: "—"

  coverage:
    available: false
    command: "—"

  quality:
    linter:
      available: false
      command: "—"
    type_checker:
      available: false
      command: "—"
    formatter:
      available: false
      command: "—"
